import React from "react"
import vectors from '../images/vectors_gradient.svg';

const GradientBar = () => {
    return (
        <figure className="fixed w-[450px] z-0">
            <img src={vectors} alt="Vectors"/>
        </figure>
    )
}

export default GradientBar
